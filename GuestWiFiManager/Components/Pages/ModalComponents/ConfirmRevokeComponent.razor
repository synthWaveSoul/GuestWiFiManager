<div class="col-7 col-md-9">Are you sure you want to revoke Internet access for</div>
<div class="text-end" style="font-weight:700">@Name</div>
<div class="text-end">?</div>

<Button Color="ButtonColor.Secondary" @onclick="revokeAccessConfirmed">Confirm</Button>

@code {
    [Parameter]
    public string Name { get; set; }

    // [Parameter]
    // public string Id { get; set; }

    // [Parameter]
    // public Task revokeAccessConfirmed {get; set;}

    [Parameter]
    public EventCallback<MouseEventArgs> revokeAccessConfirmed { get; set; }
}
